# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
#
# `gen/` 里是 `tauri android init` 生成的 Android 工程（本机实测 46M，绝大部分是
# `app/build/` 的编译产物），以及 capabilities 的自动补全 schema。
#
# **整个忽略**的三个理由：
# 1. 体积 —— 46M 的生成物对公开仓库是纯噪音
# 2. 它**内嵌 identifier**（`applicationId` / `namespace` / Java 包目录名都由它派生），
#    所以项目改名后必须**重新生成**，而不是手改这几十个文件
# 3. 可随时重建：`pnpm tauri android init`
#
# 注意 src-tauri/gen/ 里没有 keystore 或口令（已核查），所以这不是安全问题 ——
# 纯粹是体积与「不该提交生成物」。
/gen/
