# Copy to `.env` (or `.env.local`) and adjust.
#
# ⚠️ All values here are exposed to the frontend bundle (Vite `VITE_` prefix) and
# are inlined at BUILD TIME — never put secrets here.
#
# 这里没有、也永远不会有 API Key。三个数据源密钥（Helius / Etherscan / AI）由用户
# 在设置页填写，存在 Rust 独占的 SQLite 的 `secret` 表里（设计文档 §11.1）。
# 把 Key 写进 VITE_* 等于把它打进安装包、发给每一个用户。

# 强制进入演示数据模式：读 src/fixtures/ 的样本，不发任何网络请求。
# 无密钥时产品本来就是演示模式，这个开关只是让开发时能主动进。
# VITE_FORCE_DEMO_MODE=1

