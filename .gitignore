# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files
#
# ⚠️ 这里忽略 `.env` 是因为**任何**写进去的东西都会被 Vite 在构建期内联进 JS 包，
# 等于发给每一个用户。三个数据源 API Key 一律不进这里 —— 它们存在 Rust 独占的
# SQLite（`secret` 表）里，由用户在设置页填写。见设计文档 §11.1。
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

